name: CD

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'deploy/**'
      - '.github/workflows/**'

jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          script_stop: true
          script: |
            set -euo pipefail
            USER_NAME=$(whoami)
            # Ensure ownership to avoid git permission errors
            sudo chown -R "$USER_NAME":"$USER_NAME" /opt/Footactique || true
            git config --global --add safe.directory /opt/Footactique || true
            cd /opt/Footactique
            git fetch origin
            git reset --hard origin/main
            cd deploy
            sudo docker compose build --pull web api
            sudo docker compose up -d web api
            sudo docker compose ps | cat


